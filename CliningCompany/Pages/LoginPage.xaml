<Page x:Class="CliningCompany.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Вход" Background="LightGray">
    <Grid>
        <Grid Width="300" Height="200" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Логин:" Margin="5" VerticalAlignment="Center"/>
            <TextBox x:Name="txtUsername" Grid.Row="0" Grid.Column="1" Margin="5" Padding="3"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Пароль:" Margin="5" VerticalAlignment="Center"/>
            <PasswordBox x:Name="txtPassword" Grid.Row="1" Grid.Column="1" Margin="5" Padding="3"/>

            <Button x:Name="btnLogin" Content="Войти" Grid.Row="2" Grid.ColumnSpan="2" Width="100" Margin="5" Click="btnLogin_Click"/>
            <Button x:Name="btnRegister" Content="Регистрация" Grid.Row="3" Grid.ColumnSpan="2" Width="100" Margin="5" Click="btnRegister_Click"/>
        </Grid>
    </Grid>
</Page>